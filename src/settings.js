/**
 * 系统配置项
 * 定义系统的基础配置信息
 */
module.exports = {
  // 系统标题
  title: 'Vue Admin Template',

  /**
   * 是否固定顶部导航栏
   * true - 固定在顶部（滚动时始终显示）
   * false - 不固定（随页面滚动）
   */
  fixedHeader: false,

  /**
   * 是否在侧边栏显示 Logo
   * true - 显示 Logo 图片和标题
   * false - 不显示 Logo
   */
  sidebarLogo: false
}
